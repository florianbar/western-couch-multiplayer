t_draw_sprite_fade();

draw_self();

// Use this for testing to see if the trail starts at the right position, behind the spaceship
//draw_sprite_ext(sprite_index, 0, x, y, 1, 1, image_angle, c_white, 0.2);